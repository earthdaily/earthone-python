This package implements the cli support for auth.

It is not part of the `earthone.auth` package to avoid injecting unnecessary dependencies everywhere due to the widespread use of `earthone.auth` everywhere.
